spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medibridge_user_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Prakash@1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: OmprakashreddySaggam@gmail.com
    password: frgizhomwrdyatoe
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

management:
  health:
    mail:
      enabled: false

jwt:
  secret: dev-secret-key-change-this-in-production-minimum-256-bits
  expiration: 86400000
  refresh-expiration: 604800000

logging:
  level:
    root: INFO
    com.medibridge: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
